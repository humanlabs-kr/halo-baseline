/**
 * Re-exported so the queue keeps its local import path. The value lives in
 * `@halo/contracts` because the mini app prints it, and two copies of a number
 * the user can check is how the rewards screen came to advertise "+30".
 */
export { BASE_POINT_PER_RECEIPT } from '@halo/contracts';
